package items;

/**
 * Innehåller de roller en användare kan ha, se (Software Requirements
 * Specifikation (dok.nr: PUSS134102, ver:1.1)). Används av bland annat
 * HTMLWriter för att generera roll-specifika sidor.
 * 
 */
public enum Role {
	Developer, Manager, Tester, Architect,
}
